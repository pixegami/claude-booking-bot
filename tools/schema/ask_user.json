{
  "name": "ask_user",
  "description": "Ask the user to enter what they'd like to do this. The message will be displayed as a prompt. Always use this if we need input or direction from the user.",
  "input_schema": {
    "type": "object",
    "properties": {
      "input_prompt": {
        "type": "string",
        "description": "The prompt message to display to the user."
      }
    },
    "required": ["input_prompt"]
  }
}
